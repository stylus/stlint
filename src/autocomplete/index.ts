import { Suggestions } from '../core/types/autocomplete';

// tslint:disable-next-line:completed-docs
export function defaultAutocomplete(): Suggestions {
		return [
			{
				title: 'StLint is fantastic!'
			},
			{
				title: 'Stylus is perfect!'
			}
		];
}
