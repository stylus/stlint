test = #CCC
$colors = {
	grey: #CCC #FFF #F00
}

p = {
	optionColor: #FFF
}

.b-checkbox-list
	position absolute
	right basis(1.5)
	left basis(1.5)

	font-size basis(1.5)

	color $p.optionColor
	background-image url('test.png')

.w
	color $colors.grey[1]
