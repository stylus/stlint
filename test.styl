@import "test2.styl"

$p = {
	colors: #CCC #ddd #eee #cdd
	width: basis(10, 20px, #ccc) basis(125) solid
	color: {
		fff: black red
	}
	background: $colors.white[0]
	offersShopsText: $colors.grey[2]
	discountBgColor: $colors.base.third
}

b-retailer extends i-block
	overflow hidden
	border $p.border

	color #ccc
