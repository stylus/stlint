test = #CCC
$colors = {
	grey: #CCC #FFF #F00
}

p = {
	optionColor: #FFF
}

.b-checkbox-list
	position absolute
	right basis(1.5)
	left basis(1.5) $colors.grey[0]

	size 12px

	font-size basis(1.5)

	color $p.optionColor
	background-image url('test.png')

.w
	color $colors.grey[1]
